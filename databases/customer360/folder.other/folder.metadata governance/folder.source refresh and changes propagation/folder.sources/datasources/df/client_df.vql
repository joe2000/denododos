# REQUIRES-PROPERTIES-FILE - # Do not remove this comment!
CREATE OR REPLACE DATASOURCE DF client_df
    FOLDER = '/other/metadata governance/source refresh and changes propagation/sources'
    IGNOREMATCHINGERRORS = FALSE
    ROUTE LOCAL 'LocalConnection' '${databases.customer360.folder.other.folder.metadata governance.folder.source refresh and changes propagation.folder.sources.datasources.df.client_df.ROUTE.LOCAL.PATH}'
    COLUMNDELIMITER = ';'
    ENDOFLINEDELIMITER = '\n'
    HEADER = TRUE;


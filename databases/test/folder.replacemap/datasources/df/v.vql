# REQUIRES-PROPERTIES-FILE - # Do not remove this comment!
CREATE OR REPLACE DATASOURCE DF v
    FOLDER = '/replacemap'
    ROUTE LOCAL 'LocalConnection' '${databases.test.folder.replacemap.datasources.df.v.ROUTE.LOCAL.PATH}'
    COLUMNDELIMITER = ','
    ENDOFLINEDELIMITER = '\n'
    HEADER = TRUE;


# REQUIRES-PROPERTIES-FILE - # Do not remove this comment!
CREATE OR REPLACE DATASOURCE DF old
    FOLDER = '/seiglobalnewandold'
    ROUTE LOCAL 'LocalConnection' '${databases.test.folder.seiglobalnewandold.datasources.df.old.ROUTE.LOCAL.PATH}'
    COLUMNDELIMITER = ','
    ENDOFLINEDELIMITER = '\n'
    HEADER = TRUE;

